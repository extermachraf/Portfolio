/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'],
    },
};

// module.exports = {
//     images: {
//         domains: ['images.unsplash.com'],
//     },
// };

export default nextConfig;
